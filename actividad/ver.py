def visualizar_usuario(
        user:
    
) -> str:
    """
    Función que ingresa el nombre del usuario
    
    --parametros--
    -user (str): ingresa el nombre del usuario que quiere ver
     
    - return(user)
    """
user = str(input("Ingrese el nombre del usuario que quiere ver:"))
